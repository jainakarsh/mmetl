name: tabular_only
resources: {}
tasks:
  - id: extract
    uses: noop.adapter
    outputs: [rows]
    config: {}
  - id: transform
    uses: noop.transform
    inputs: [rows]
    outputs: [clean_rows]
    config: {}
  - id: load
    uses: noop.adapter
    inputs: [clean_rows]
    config: {}
